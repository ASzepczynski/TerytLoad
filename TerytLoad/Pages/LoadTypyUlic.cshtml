@page
@model TerytLoad.Pages.LoadTypyUlicModel
@{
    ViewData["Title"] = "Ładowanie TypyUlic";
}

<h1>@ViewData["Title"]</h1>

<div class="card mb-4">
    <div class="card-header">
        <h5 class="mb-0">Ładowanie TypyUlic z słownika do bazy danych</h5>
    </div>
    <div class="card-body">
        <p class="text-muted">
            Ta funkcja porównuje wszystkie wpisy z tabeli <strong>TerytUlic</strong> z tabelą
            <code>TerytUlicPoprawki</code> i ładuje ich unikalne wystąpienia do tabeli <strong>TypyUlic</strong>.
            Wcześniej ładuje tabele z arkuszy Excel z AddressLibrary/AppData/Dictionaries: <strong>CechyUlic</strong> i <strong>TytulyStopnie</strong>.
            Logi są zapisywane do folderu <code>AppData/Logs</code>: <code>LoadTypyUlic.txt</code> <code>LoadCechyUlic.txt</code> i <code>LoadTytulyStopnie.txt</code>.
        </p>

        <form method="post" id="loadForm">
            <button type="submit" class="btn btn-primary btn-lg" id="loadButton">
                <i class="bi bi-download"></i> Rozpocznij ładowanie
            </button>
        </form>
    </div>
</div>

<!-- Pasek postępu (ukryty domyślnie) -->
<div id="progressContainer" style="display: none;" class="mb-4">
    <div class="alert alert-info">
        <h4 id="progressTitle">⏳ Trwa ładowanie TypyUlic...</h4>
        <div class="progress mt-3" style="height: 30px;">
            <div id="progressBar"
                 class="progress-bar progress-bar-striped progress-bar-animated"
                 role="progressbar"
                 style="width: 0%;"
                 aria-valuenow="0"
                 aria-valuemin="0"
                 aria-valuemax="100">
                0%
            </div>
        </div>
        <div id="progressMessage" class="mt-3" style="font-family: monospace; font-size: 14px; white-space: pre-wrap;">
            Inicjalizacja...
        </div>
        <div id="progressStats" class="mt-2 text-muted">
            <small>
                Przetworzono: <strong><span id="processedCount">0</span></strong> /
                <strong><span id="totalCount">0</span></strong>
            </small>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/@@microsoft/signalr@latest/dist/browser/signalr.min.js"></script>
    <script>
        const connection = new signalR.HubConnectionBuilder()
            .withUrl("/progressHub")
            .withAutomaticReconnect()
            .configureLogging(signalR.LogLevel.Information)
            .build();

        connection.on("ReceiveProgress", (operation, current, total, message) => {
            if (operation !== "load-typy-ulic") return;

            const progressContainer = document.getElementById("progressContainer");
            const progressBar = document.getElementById("progressBar");
            const progressMessage = document.getElementById("progressMessage");
            const progressTitle = document.getElementById("progressTitle");
            const processedCount = document.getElementById("processedCount");
            const totalCount = document.getElementById("totalCount");
            const loadButton = document.getElementById("loadButton");

            progressContainer.style.display = "block";

            const percentage = total > 0 ? Math.round((current / total) * 100) : 0;

            progressBar.style.width = percentage + "%";
            progressBar.textContent = percentage + "%";
            progressBar.setAttribute("aria-valuenow", percentage);

            progressMessage.textContent = message;

            processedCount.textContent = current.toLocaleString('pl-PL');
            totalCount.textContent = total.toLocaleString('pl-PL');

            if (message.startsWith("✅")) {
                progressBar.classList.remove("progress-bar-animated", "progress-bar-striped");
                progressBar.classList.add("bg-success");
                progressTitle.textContent = "✅ Zakończono pomyślnie!";
                loadButton.disabled = false;
                progressContainer.querySelector('.alert').classList.remove('alert-info');
                progressContainer.querySelector('.alert').classList.add('alert-success');
            }

            if (message.startsWith("❌")) {
                progressBar.classList.remove("progress-bar-animated", "progress-bar-striped");
                progressBar.classList.add("bg-danger");
                progressTitle.textContent = "❌ Wystąpił błąd";
                loadButton.disabled = false;
                progressContainer.querySelector('.alert').classList.remove('alert-info');
                progressContainer.querySelector('.alert').classList.add('alert-danger');
            }
        });

        connection.start()
            .then(() => console.log("SignalR Connected"))
            .catch(err => {
                console.error("SignalR Connection Error: ", err);
                alert("Nie można połączyć z serwerem SignalR. Odśwież stronę i spróbuj ponownie.");
            });

        document.getElementById("loadForm").addEventListener("submit", function () {
            const loadButton = document.getElementById("loadButton");
            const progressContainer = document.getElementById("progressContainer");
            const progressBar = document.getElementById("progressBar");
            const alertDiv = progressContainer.querySelector('.alert');

            loadButton.disabled = true;
            progressContainer.style.display = "block";

            progressBar.style.width = "0%";
            progressBar.textContent = "0%";
            progressBar.classList.add("progress-bar-animated", "progress-bar-striped");
            progressBar.classList.remove("bg-success", "bg-danger");

            alertDiv.classList.remove('alert-success', 'alert-danger');
            alertDiv.classList.add('alert-info');

            document.getElementById("progressTitle").textContent = "⏳ Trwa ładowanie TypyUlic...";
            document.getElementById("progressMessage").textContent = "Wysyłanie żądania do serwera...";
        });
    </script>
}
